import 'package:flutter/material.dart';

class MessAdminDashboard extends StatelessWidget {
  const MessAdminDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Mess Admin Dashboard")),
    );
  }
}
